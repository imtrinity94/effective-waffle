name: Shell-Command

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps: 
      - name: echo a string
        run: echo "Helo World"
      - name: multiline script
        run: |
          npm -v
          node -v

 
